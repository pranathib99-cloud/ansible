- name: vers preference
  hosts: localhost
  connection: local
  vars:
   NAME: from play level
  vars_files:
    - course.yaml
  vars_prompt:
  - name: NAME
    prompt: "Enter your name"
    private: false
  tasks:
  - name: print the variable preference
    ansible.builtin.debug:
      msg: "NAME is: {{ NAME }}"