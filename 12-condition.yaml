- name: conditions 
  hosts: localhost
  connection: local
  vars:
   number: 5
  tasks:
  - name: check if number is less than 10
    ansible.builtin.debug:
     msg: "given number is {{ number }} less than 10"
    when: number < 10 